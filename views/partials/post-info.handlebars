<article>
  <div class="title">
    <a class="blog-title" href="/post/{{id}}" > {{ title }} </a>
    <p>Posted by {{ user.username }} on {{ format_date created_at }}</p>
  </div>

    <p>{{body}}</p>
</article>

